#include "main.h"
/**
 * add - Add function that adds two aguments
 * Return: return value of addition of two aguments
 * @first: stores first argument
 * @sec: stores second argument
 */

int add(int first, int sec)
{
	int addition = first + sec;

	return (addition);
}
